# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Database Configuration (PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/breachbuddy

# Security / Authentication
JWT_SECRET=breachbuddy_super_secret_jwt_key_2026_change_in_production
JWT_EXPIRES_IN=7d

# LLM API Configuration (Google Gemini API / Optional)
# If omitted or left empty, the server automatically uses the built-in Mock AI Service
LLM_API_KEY=
LLM_MODEL=gemini-2.5-flash
